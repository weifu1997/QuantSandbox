# Phase 1 API Reference

> Historical note: the earlier workflow / watchlist API has been removed from the active alpha codebase.

Current active API surface is centered on:
- `/api/config`
- `/api/meta`
- `/api/summary` and `/api/detail/{ticker}`
- `/api/strategies/*`
- `/api/mx/*`
- `/api/factors`
